# ── AI 탐지 ──────────────────────────────────────────────────────────
CONFIDENCE_THRESHOLD = 0.25   # 이 값 미만의 탐지 결과는 무시한다

# ── 렌더링 ───────────────────────────────────────────────────────────
RENDER_SCALE_BASE = 1000      # 폰트/박스 크기를 해상도에 비례시킬 때 기준값
FONT_SIZE_BASE    = 24        # 기준 해상도에서의 폰트 크기 (px)
FONT_SIZE_MIN     = 20        # 저해상도에서도 보장할 최소 폰트 크기 (px)
BOX_WIDTH_BASE    = 3         # 기준 해상도에서의 탐지 박스 테두리 두께 (px)
BOX_WIDTH_MIN     = 2         # 최소 박스 테두리 두께 (px)
LABEL_H_BASE      = 35        # 기준 해상도에서의 라벨 높이 (px)
LABEL_H_MIN       = 30        # 최소 라벨 높이 (px)

# ── 로그 ─────────────────────────────────────────────────────────────
TRACK_ID_WIDTH = 3            # 로그에서 track_id를 표시할 자릿수 (예: 001, 012)
